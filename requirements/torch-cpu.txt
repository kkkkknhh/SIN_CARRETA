# PyTorch CPU-only installation
# For systems without GPU or for development/testing
# ============================================================================

-r base.txt

# PyTorch CPU variant (significantly smaller download)
torch==2.1.2+cpu
torchvision==0.16.2+cpu
torchaudio==2.1.2+cpu

# Note: Install with:
# pip install -r requirements/torch-cpu.txt --extra-index-url https://download.pytorch.org/whl/cpu
