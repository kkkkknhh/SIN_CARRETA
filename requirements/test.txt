# Testing-Only Dependencies
# Minimal set for running tests in CI/CD environments
# ============================================================================

-r torch-cpu.txt  # CPU-only for faster CI

# Testing Framework
pytest==7.4.3
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-timeout==2.2.0
pytest-asyncio==0.21.1

# For API testing
httpx==0.25.2

# Coverage reporting
coverage[toml]==7.3.4
