# buf.work.yaml
# Buf workspace configuration for multi-module projects
version: v1

directories:
  - proto

# This file enables Buf workspace mode, allowing multiple modules
# to be managed in a single repository. Currently we have one module
# but this allows for future expansion.
